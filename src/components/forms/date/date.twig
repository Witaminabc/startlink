{% include 'input.twig' with {
    modify: 'date-filter form-input_date form-input_date-start',
    name: 'date[start]',
} %}
{% include 'input.twig' with {
    modify: 'date-filter form-input_date form-input_date-end',
    name: 'date[end]',
} %}